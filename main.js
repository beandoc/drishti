document.addEventListener('DOMContentLoaded', () => {
    console.log('Nav-Drishti Hero Section Loaded');
    
    // Add interactions if needed
    const ctaPrimary = document.querySelector('.btn-cta-primary');
    ctaPrimary.addEventListener('click', (e) => {
        // e.preventDefault();
        console.log('Explore Conditions clicked');
    });
});
